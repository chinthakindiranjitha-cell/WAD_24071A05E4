# LearnSphere

Simple React-based LearnSphere exam portal with client-side authentication, subjects, mock tests, score card and contact pages.

Run locally:

```bash
npm install
npm run dev
```

Open http://localhost:5173 in your browser (Vite default).

All styling is in `src/App.css` and components are in `src/components`.
